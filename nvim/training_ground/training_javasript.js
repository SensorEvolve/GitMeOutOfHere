function calculateArea(length, width) {
    return length * width;
}

let length = 5.5;  // Length in meters
let width = 3.5;   // Width in meters
console.log(`The area of the floor is ${calculateArea(length, width)} square meters.`);
console.log(:
